# Copy to .env (or set these in your deployment environment). Never commit .env.
# Generate a fresh key with:
#   python -c "from django.core.management.utils import get_random_secret_key as g; print(g())"
DJANGO_SECRET_KEY=replace_me_with_a_freshly_generated_key
